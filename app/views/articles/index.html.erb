<h2>All my articles </h2>
<table>
	<tr>
		<th>Title</th>
		<th>Text</th>
	</tr>

	<% @articles.each do |article| %>
		<tr>
			<td><%= article.title %></td>
			<td><%= article.text %></td>
		</tr>
	<% end %>
</table>
<br>
<%= link_to 'Go back to home', root_path %>